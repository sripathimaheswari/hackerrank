if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    arr=list(arr)
    m=max(arr)
    l=[]
    for i in arr:
        if(i!=m):
            l.append(i)
    r=max(l)
    print(r)
